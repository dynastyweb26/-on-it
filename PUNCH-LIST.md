# On It — Punch List

_Audited from the code on 2026-09-07 (branch `main`). Status is one of **Done**,
**Partial**, **Open**, **Unverified** (can't be determined from the repo alone —
e.g. live DB/runtime state), or **Not Reproducible** (tried to reproduce the
reported symptom and could not). Evidence is `file:line` or a commit hash.
Trust this over ONIT-SPEC.md where they disagree; the spec is stale._

## Invariants

_Properties that must hold. Each has been verified in code, never assumed — if
you touch the relevant area, re-verify rather than trusting this line._

1. **Intent is owned by the draft.** It changes only when the user explicitly
   names the document type (quote/invoice/bill/estimate) and is **never**
   overwritten by a parse. Enforced by `intent_explicit` (`src/lib/ai.ts`) +
   the functional `setDraft` merge in `chat/page.tsx` that keeps the prior
   intent when `intent_explicit` is false. (`11f5c4a`, `6f2dd00`)
2. **RLS is the sole server-side auth boundary.** Row-level security on every
   user-data table (owner-only, scoped to `auth.uid()`) is what actually
   protects data — verified intact by policy review + anonymous REST probes.
   There is no server-side route-gating middleware. `src/middleware.ts` exists
   again but is **refresh-only** (`@supabase/ssr` updateSession — it rotates the
   session cookie and does NOT redirect; see the middleware Bug row). The
   per-page client redirects to `/login` are UX only, not a security boundary.
   RLS is verified, never assumed.

## Bugs

| Item | Status | Evidence |
|---|---|---|
| middleware cookie write-back | **Resolved — refresh-only middleware restored** (`fix/middleware`) | History: the root `middleware.ts` was inert (Next.js loads `src/middleware.ts` only); relocating it into `src/` caused a signed-in login loop twice (`c2daaa4`→`8392ada`, `2734494`→`353190a`) and it was then deleted (`c9be344`). **Root cause of the loop (now identified): the dropped-cookie bug.** Its redirect branch did `return NextResponse.redirect(...)`, a fresh response that never received the cookies written by `setAll`; when `getUser()` rotated the refresh token the rotated cookie was discarded, the browser kept the consumed token, the next `getUser()` failed, and a signed-in user was bounced to `/login` — the SAME failure documented and fixed in `src/app/auth/confirm/route.ts`. **Fix:** restore `src/middleware.ts` as **refresh-only** (`@supabase/ssr` updateSession) — it refreshes the cookie and always returns the one response object; it has **no redirect branch**, so the loop is structurally impossible. Matcher excludes `login`, `auth/`, `i/`, the cron routes, the Stripe webhook, `_next`, manifest/sw/icons/brands, and static files. Auth stays enforced by RLS + per-page client guards; `/chat` guests untouched. Commit `8adf1cd`. The push sanity check is now `Test-Path src/middleware.ts` = **True** (was False while deleted). |
| summary getUser stall | **Open** | `src/app/(app)/summary/page.tsx:52-53` gates on the network `getUser()` with **no** `getSession()` fast-path and **no** timeout — the same stall settings had. The settings fix (`09dcc22`) was never ported here. |
| settings unauthenticated loading | **Done** | `09dcc22` — `src/app/(app)/settings/page.tsx` added a local `getSession()` fast-path that redirects a signed-out user to `/login` instantly instead of hanging on "Loading…". |
| duplicate warning loop | **Done** | Resolved by making the warning non-blocking (`71ed4c1`): the server's `duplicateWarning` signal (unchanged, `src/app/api/parse/route.ts`) renders as a passive gold badge — "Similar invoice sent recently" — on the still-actionable invoice card. No turn is spent and no confirmation is asked, so there is no loop to re-fire. The earlier ack-based approach (`dupAcked` / `finalize` bypass / `pending`) was superseded and removed as dead code in the follow-up cleanup. |
| client name truncation | **Not Reproducible** | Voice repro on 2026-09-07 returned "Cyril" correctly end to end — transcript, parse, and card all intact. The earlier "Cyi" for "Cyril" symptom did not recur. Reopen with a fresh repro if it resurfaces (candidate sites: `src/app/api/transcribe/route.ts`, `src/lib/ai.ts`). |
| voice end-of-turn affordance | **Done** | `56d0c3d` — listening copy now reads "Listening… tap the mic when you're done." so the user knows to tap the mic to have On It process the turn. The recording state was already visually distinct (`.voice-listening` pulse + "Stop and send" aria-label). |
| hardcoded invoice language (greeting/CTA/"ready to send") | **Done** | Greeting made neutral in `d5f9c9d` ("…I'll take care of the rest.", was "…I'll handle the invoice."). The rest were already kind-aware: send CTA "Looks right — send it" (`chat/page.tsx:1549`, `de407ee`); confirm summary + "…is sent" line via `${kind}` (`chat/page.tsx:1020`, `confirmSummary`); preview card title (`chat/page.tsx:1540`); download message names no kind (`chat/page.tsx:1098`). |
| Cron auth failed open on unset `CRON_SECRET` | **Fixed — pending merge** (`fix/small-batch`) | `/api/followups` and `/api/trial-reminders` compared the header against `` `Bearer ${process.env.CRON_SECRET}` ``; with the secret unset/blank this became the literal `"Bearer undefined"`/`"Bearer "`, a value an attacker could send verbatim to pass. It was also non-constant-time. New `src/lib/cron-auth.ts` `verifyCronAuth()` fails closed on an unset/blank secret, length-checks, then `timingSafeEqual` (`node:crypto`); `import 'server-only'` keeps the secret out of any client bundle. Both routes call it; `lib/stripe` untouched. No test file (no runner). Commit `db87d64`. Aligns with the SECURITY.md "Cron auth" layer. Fixed 2026-09-17. |
| PDF totals block left-aligned on multi-page continuation pages | **Fixed — pending merge** (`fix/small-batch`) | On multi-page documents the totals block rendered left-aligned on page 2 while page 1 / single-page right-aligned. `lib/pdf/generate.ts` clones each `data-pdf-block` onto the final page inside a column-flex wrapper that lacks the template's `justifyContent:'flex-end'`, so the fixed-width (300px) totals fell left. Fixed at the template level: the shared `Totals` block (Classic/Industrial/Friendly) gets `marginLeft:auto` so it right-aligns from its own style in both the page-1 row and the page-2 column; Ledger's own totals block got the same for consistency (Ledger totals are not paginated to a continuation page — no behavior change). No `generate.ts` change. Commit `99ef399`. Fixed 2026-09-17. |

## Infra

| Item | Status | Evidence |
|---|---|---|
| turn trace logging | **Open** | Only failure logging exists — `console.error` at `src/app/api/parse/route.ts:94` and `src/lib/ai.ts:118`. No structured per-turn trace (no trace id, no request/response logging). |

## Polish

| Item | Status | Evidence |
|---|---|---|
| splash screen | **Done** | `src/components/Splash.tsx`, mounted at `src/app/layout.tsx:67`; `@keyframes splash-in` at `globals.css:131`; shows once per session (`sessionStorage`). |
| gold ring on selected states | **Done** | `.ring-gold-selected` defined `globals.css:62`; used on template/color/background selections in `settings/page.tsx`. |
| emoji removal incl. AI system prompt | **Done** | 0 pictographic emoji across `src/` (scanned). AI prompt bans them: `src/lib/ai.ts:45` ("no emoji, ever") and `:46` ("Never use emojis anywhere in your replies."). |
| logo size in the 4 PDF templates | **Done** | Each template sizes the logo and shrinks the business name when a logo is present: `src/lib/pdf/templates/index.tsx` heights 128/96/140/128 at `:325`, `:523`, `:574`, `:629`. |
| sidebar invoice template | **Done** | Implemented as "Ledger" (DB key kept as `sidebar`) — `templates/index.tsx:374`, registered `TEMPLATES` `:703`, label `:713`. |

## Features

| Item | Status | Evidence |
|---|---|---|
| voice mode redesign | **Partial / Unverified** | Push-to-talk voice session is implemented inline in chat: `recorderRef`/`MediaRecorder`, `startRecording` (`chat/page.tsx:1378`), `micTap` (`:1428`), transcribe→parse→speak, mic-blocked fallback (`:1424`). The header comment claims a "full-screen voice mode" (`:6`). Functionally present; whether it matches the intended **redesign** can't be confirmed from code. |
| swipe tab nav | **Done** | `src/app/(app)/layout.tsx` — `onTouchStart/Move/End` with `SWIPE_THRESHOLD` (`:22`, `:83-107`); vertical-scroll gesture wins. |
| tutorial walkthrough cards | **Done** | `src/components/tutorial/` — `FirstRunTutorial.tsx`, `SlideCarousel.tsx`, `TutorialReference.tsx`, `slides.tsx`, `persistence.ts`; auto-show gated on onboarded + version in `layout.tsx:53-64`. |
| recurring invoices | **Open** | No implementation. "recurring" appears only in subscription billing copy (`settings/page.tsx:605`, `PaywallModal.tsx:118`). |
| signature capture | **Open** | No implementation. "signature" appears only in Stripe webhook verification (`api/webhooks/stripe/route.ts`). |
| pay page `/pay/[token]` + public card payment route | **Open (deferred)** | No `/pay` route exists and no public card-payment endpoint exists; both deferred. Backend scaffolding is present but **unused**: `get_public_invoice(text,text)` and the invoice token (`gen_invoice_token`) in migration `20260901120000_payment_methods_and_public_invoice.sql`; no app code calls either. |

## Verify

| Item | Status | Evidence |
|---|---|---|
| conversation state persistence across app switch | **Done** | `StoredChat` persisted to `localStorage`; `restoreFromStore()` (`chat/page.tsx:347`) runs on mount, `visibilitychange` (`:455`), and `pageshow` (`:456`); `applyStoredChat` rehydrates messages/draft/ready/pending/dupAcked. |
| retry on failure message | **Done** | `Failure` type (`chat/page.tsx:30`) on `Msg.failed`; failed messages render an icon retry button (`:1504-1519`); `retry()` (`:1128`) re-runs `send`/`finalize` and replaces the failed bubble in place (`emitResult`, `:68`). |
| migration 003 applied | **Done** | Applied via the Supabase SQL Editor on 2026-09-07. File: `supabase/migrations/003_relax_website_check.sql` — drops the http(s):// check, re-adds a `char_length(...) <= 200`-only constraint, so bare domains (e.g. `vtcprojects.com`) are accepted. |

## Open — logged 2026-09-15 (product review)

_Newly logged this date. Items marked "verified 2026-09-15" were confirmed in
code during that audit; the rest are reported symptoms not yet code-verified —
re-verify before acting, per the note under Invariants._

| Item | Status | Evidence |
|---|---|---|
| Protected pages with no signed-out redirect | **Fixed — pending merge** (`fix/middleware`) | `dashboard`, `invoices`, `invoices/[id]`, `expenses`, `vault` had no mount-time auth guard — a signed-out user saw an empty shell instead of `/login` (RLS protected the data regardless). Added the same guard `summary`/`settings` use: `getSession()` (no-network local read) then `getUser()`, `router.replace('/login')` when either is absent. Middleware is refresh-only and never redirects, so this is the redirect UX. `/chat` deliberately still open to guests. Commit `cf808cb`. Was: verified 2026-09-15; fixed 2026-09-17. |
| Narration total disagrees with the card | **Fixed — merged to main** (was `fix/money-narration`) | Two causes, both closed. (1) `confirmSummary` summed `qty*unit_price` only — no tax, no deposit — so the spoken confirmation was the bare subtotal; now routed through `calculateInvoiceTotals` with the draft's tax_rate/deposit_type/deposit_value (`02bb51f`). (2) The `/api/parse` `reply` was model-authored and spoken verbatim, free to state any figure; the server now strips any sentence containing a "$" amount and appends one deterministic sentence from `calculateInvoiceTotals`, and the prompt forbids the model from stating amounts (`82ac06d`). Verified 2026-09-15. **Re-audit 2026-09-17 (`fix/punch-batch-1`): both commits confirmed ancestors of `main`; every money-narration path routes through `calculateInvoiceTotals` — `confirmSummary` (`chat/page.tsx:986`), `buildRenderData` (`:932`, `ba8c15b`), the parse money sentence (`route.ts:198`), and the preview-card headline (`:1863`). The only spoken text is the parse reply (`:773`), whose figure comes from the same helper. No path bypasses it; no code change needed.** |
| Follow-up push copy: raw total, stale "mark paid" | **Fixed — pending merge** (`fix/money-narration`) | `api/followups/route.ts` formatted the raw `$${inv.total}` and said "resend or mark paid" — mark-paid is being removed (paid-ness follows the ledger, `20260918000003_paid_status_from_ledger.sql`) and the push has no such action. Now selects `amount_paid`, computes the remaining balance via `roundCurrency`, skips fully-covered invoices, and formats with `money()`: "Invoice #{n}: {money(balance)} still due. Tap to view." (`3d528dd`). Verified 2026-09-15. |
| Chat-requested deposits become line items — root cause: `ExtractResult` has no deposit fields | **Fixed — pending merge** (`fix/deposit-parsing`) | `ExtractResult` (`src/lib/ai.ts:10-38`) declares no `deposit_type`/`deposit_value`, and the prompt routes deposit terms into `notes` (`ai.ts:57`); combined with the rule "a total price for the whole job → one line item" (`ai.ts:50`), a spoken deposit like "500 down" has no structured home and gets parsed as a $500 line item. The render and summary paths already read `(draft as any).deposit_type`/`deposit_value` (`chat/page.tsx:930-931`, and `route.ts:195-196`), but nothing populates them from the parse. **Additional finding (`fix/punch-batch-1` audit 2026-09-17): the client merge `setDraft((prev) => … return mergedDraft)` (`chat/page.tsx:863-868`) replaces the whole draft with the parse result, which has no deposit fields — so a deposit set via the card's UI control (`applyDraftDeposit`) is silently wiped on the very next chat turn. The fix must preserve the draft's deposit when the parse doesn't restate one.** Fixing it spans schema + prompt + route normalization + client draft merge (DB columns already exist: `20260915000000_deposit_support.sql`, `20260916000000_deposits_and_partial_payments.sql`) and needs manual testing — no test runner. **Decision 2026-09-17: kept OUT of `fix/punch-batch-1`; do it on its own branch `fix/deposit-parsing`, next.** **Fixed 2026-09-17 (`fix/deposit-parsing`), two commits.** (1) Client merge (`f0b9d17`): `mergedDraft` no longer replaces the whole draft — deposit_type/deposit_value and notes are merged, so the card-set deposit survives the next chat turn (the wipe). Per-turn signal: deposit_type `null` keeps the draft's, `'none'` clears, `'percentage'`/`'fixed'` + a value > 0 sets it (a bare type with no usable value is treated as unstated, so a model echo can't zero the amount); notes `null` keeps, `''` clears, any string sets. (2) AI + route (`563fcbc`): `ExtractResult` gains `deposit_type`/`deposit_value` (+ schema string + clarify fallback); the prompt adds a deposit rule (structured, never a line item or note), the notes rule no longer captures deposit terms, and the total-price rule excludes deposits ("fence job 1000, 500 down" → one $1000 line item + fixed $500 deposit). `route.ts` normalizes/bounds the deposit (stated type needs a usable value or downgrades to null; `'none'` clears; percentage capped at 100) and the money narration computes with the merged deposit (result's when stated, else the draft's) so a deposit-only change is spoken. Emits only `'percentage'` (never `'percent'`). Typecheck clean each commit; manual test on a Vercel preview (no test runner). Verified 2026-09-15. |
| PDF date format mismatch | **Fixed — pending merge** (`fix/small-batch`) | Same defect as Jules F8 (see the 2026-09-16 Jules table): issued vs. due used different formats within one document. Both are now formatted via `formatDate()`. Commit `4249ed7`. Fixed 2026-09-17. |
| Request-balance PDFs never archived to the Vault | **Fixed — pending merge** (`fix/punch-batch-1`) | `resend(isBalanceRequest)` (`invoices/[id]/page.tsx`) generated and shared the balance-request PDF but, unlike the chat Send path (`chat/page.tsx:1424`), never wrote to `vault_documents`. Now archives on a real share (skips a cancelled share sheet) with the same storage-upload + insert. Distinguished from the original invoice by `title` ("Balance request — …") and a `balance-`-prefixed storage path — `doc_type` is a fixed enum (`invoice/quote/receipt/contract/photo/other`, `001_init.sql:105`) with no balance value, so it stays `inv.kind`; `invoice_id` ties it to the parent so a soft delete still hides it. Best-effort. A normal "Share PDF" resend re-shares the original (already archived at first send) and is left alone. Commit `aba221d`. Fixed 2026-09-17. |
| Header: show project total as a subline under "due now" | **Fixed — pending merge** (`fix/punch-batch-1`) | The invoice-detail header card (`invoices/[id]/page.tsx:433`) showed a bare `money(totals.dueNow)` with no label — for a deposit invoice that is only the deposit, for a partially-paid one the remaining balance, so the full project total was invisible. Added a muted `Total $X` subline beneath the headline, rendered only when `totals.total !== totals.dueNow` so a plain unpaid invoice (dueNow === total) never reads the same figure twice. Commit `e3520ad`. Fixed 2026-09-17. |
| `enforce_free_invoice_limit` counts soft-deleted rows | **By design (anti-abuse) — kept; UX copy added** (`fix/soft-delete`) | `20260724154509_enforce_free_invoice_cap.sql:73-75` counts all `kind='invoice'` rows with no `deleted_at` guard, so deleting an invoice cannot free space under the free cap. This is intentional — the cap is an anti-abuse limit. `src/lib/access.ts` deliberately does **not** filter `deleted_at` either, so the client-side count matches the DB trigger. **Decision 2026-09-17: the count stays as-is** (trigger and `access.ts` untouched); to stop a free user being surprised, the delete confirm modal now shows one line for invoice deletes — "On the free plan this still counts toward your invoice limit — deleting it won't free up a slot" (`DeleteConfirmModal.tsx`, `fix/soft-delete`). Verified 2026-09-15; copy added 2026-09-17. |
| Nine duplicate `money()` helpers | **Open** | Nine `money()` definitions: one canonical export (`src/lib/financials.ts:30`) plus eight inline copies — `dashboard/page.tsx`, `expenses/page.tsx`, `invoices/page.tsx`, `invoices/[id]/page.tsx`, `summary/page.tsx`, `pdf/summary-template.tsx`, `pdf/templates/index.tsx`, and `components/DateDivider.tsx`. The `chat/page.tsx` copy was dropped onto `financials.money()` with the narration fix (`02bb51f`, `fix/money-narration`). Consolidate the rest. Verified 2026-09-15; count updated 2026-09-15. |
| No test runner configured | **Deferred** | `package.json` scripts are only `dev`/`build`/`start`/`lint`; no jest/vitest/playwright in deps. Money logic (`calculateInvoiceTotals`, the narration sentence-stripping) is covered by typecheck + manual reasoning only. Adding a runner is deferred — flagged so the deposit-fields work above lands knowing there is no regression net. Verified 2026-09-15. |
| Dead space on the final PDF page | **Fixed — pending merge, awaiting preview check** (`fix/punch-batch-1`) | Root cause found in the multi-page partitioner (`lib/pdf/generate.ts`): the page 2+ loop set `isLastPageAttempt = pEnd < totalRows`, always true on entry (`pEnd = currentRow < totalRows`), so **every** continuation page reserved `bottomHeight/2` of capacity for the trailing totals block and under-filled — spreading rows thin and trailing off into a mostly-empty final page (and, when the reduced capacity rejected rows that would fit at full capacity, spilling a near-empty extra page). Fix: pack each continuation page to the full `continuationCapacity`, and reserve the full `bottomHeight` only on the page that actually consumes the last row (where Totals/Payment/Notes are appended); if that page's packed rows leave too little room, pull rows back (≥1 kept) so the block spills onto a final page that fits it — never orphaned or clipped by `overflow:hidden`. Widow guard kept for non-last pages. Commit `c474316`. **Verify on a Vercel preview with Q-0012 (two-page quote)** before merge — PDF whitespace is only observable in a render. Fixed 2026-09-17. |
| PDF font bundling (from `16621b2`) | **Deferred** | Inter base + Playfair for Ledger via `next/font`. Restyles all four templates, so needs a visual pass on each, including multi-page Ledger. Must await `document.fonts.load()` for Playfair before html2canvas capture, or the first render falls back to Times. PNG (`a957431`) already on main, dropped. |
| Unmerged work to port | **Open** | **Soft-delete + back nav is MERGED** — `feat/soft-delete-backnav` landed on `main` (merge `2a7d224`; `5a9a77a`, `fe64a06`, `472bad3`, `fa655cb`, `2f3aa88`, `631536f`). Invoices/expenses now soft-delete (`deleted_at`) with swipe + undo, and every active read filters `deleted_at is null` (audited 2026-09-17). Remaining port items: deductible removal + date dividers (`f76f8fc`, `93b5288`, `7225f75`) on `feat/books-date-dividers-port`; loading skeletons on `feat/skeleton-loading-port` (dashboard Books-totals skeleton + chat restore skeleton only — `6d1d44d` invoice-list and `2f194f6` invoice-detail were **dropped**, already on `main`). **`feat/invoice-pdf-pagination` is retired** — `a957431` (PNG) already on `main`; `16621b2` (font bundling) deferred to its own row above. The swipe threshold/velocity tuning from `2ef8490` is **dropped** — not ported by decision. Reported 2026-09-15; confirm each branch before porting. |
| Intermittent multi-second scroll/touch freeze | **Open — needs investigation** | App becomes unresponsive to scroll/touch for seconds, intermittently; got more frequent around when `feat/skeleton-loading` was built. **Not caused by the skeletons** (they are presentational, self-limiting, loading flags all clear in `finally`, no effects without cleanup, no touch-blocking CSS — audited 2026-09-15). Suspects to investigate: the layout tab-swipe `onTouchMove` handler, `SwipeableRow`, and heavy per-render effects. Needs its own investigation. |
| Delete test invoice INV-0016 from Cyril's account | **Open (manual, live DB)** | One-time cleanup: remove test invoice `INV-0016` from Cyril's account once soft-delete ships. Live-DB action, not code. Reported 2026-09-15. |

## Open — logged 2026-09-15 (tax-rate + chat/expense audit)

_Tax-rate unit traced end to end; the chat/expense items are reported symptoms,
some code-verified during the audit (marked). Re-verify before acting._

| Item | Status | Evidence |
|---|---|---|
| tax_rate parsed as a fraction → "Tax (0.08%)", $0.40 on $500 | **Fixed — pending merge** (`fix/tax-rate-unit`) | The prompt gave no unit, so the model returned `0.08` for "8 percent"; every reader treats tax_rate as percent (`financials.ts:62` `rate/100`; PDF label `` `Tax (${d.taxRate}%)` `` `pdf/templates/index.tsx:223`,`:523`). Canonical unit = **percent**. Fixed at the AI boundary only: prompt rule that tax_rate is a percent number, set only when stated (`b253b89`); route nulls a value outside `[0,100]`, no `*100` heuristic (`6e75fb6`). **Existing rows:** production is 0/77 with `tax_rate>0`, so nothing to repair today; detection query for the suspected-fraction set (review manually, do NOT blind-`*100` — a real 0.5% lives in `(0,1)`): `select id, subtotal, tax_rate, tax_amount, total from invoices where tax_rate > 0 and tax_rate < 1;`. Verified 2026-09-15. |
| No manual tax field on the card or detail page | **Open** | tax_rate has no UI writer anywhere — not on the chat preview card (only a deposit control, `chat/page.tsx:1645-1666`) nor on the invoice detail edit page (`invoices/[id]/page.tsx` edits line items + deposit, never tax_rate). The AI is the sole origin, so a misparsed or unwanted rate can't be corrected or cleared by hand. Add a tax input to at least the detail page. Verified 2026-09-15. |
| `buildRenderData` computes tax inline instead of `calculateTaxAmount` | **Open** | `chat/page.tsx:718-720` hand-rolls `taxAmount = Math.round(subtotal * taxRate) / 100` and `total = subtotal + taxAmount`, duplicating `financials.calculateTaxAmount`/`calculateInvoiceTotals` (which the very next lines then call at `:729`). Consolidate onto the helper so tax is derived one way only. Verified 2026-09-15. |
| "What's the total?" gets no number | **Open** | The AI is now barred from stating amounts (`ai.ts:47`), but nothing answers a total question deterministically — a user asking "what's the total?" gets a reply with no figure. The server should detect a total/balance question and answer it from `calculateInvoiceTotals` (same money-sentence machinery as `/api/parse`, `route.ts`). Verified 2026-09-15. |
| Expense reply says "That's logged" before the row is saved, and spells amounts in words | **Open (unverified)** | The expense confirmation copy reads as already-saved before the insert completes (risking a false "logged" on a failed save) and writes amounts as words rather than `money()` figures. Locate the expense reply/confirm path (`chat/page.tsx` expense branch, `parse-receipt`) and make the copy follow the save and use `money()`. Reported 2026-09-15. |
| Expense card needs a visible cancel (X) control | **Open (unverified)** | The expense confirmation card has no visible dismiss/cancel affordance, so a mis-triggered expense can't be backed out from the card. Add an X control on `ExpenseCard`. Reported 2026-09-15. |

## Open — logged 2026-09-16 (PDF capture + migration drift)

_PDF capture bug fixed on its own branch; the migration items are code-verified
during the 2026-09-16 audit. Re-verify before acting._

| Item | Status | Evidence |
|---|---|---|
| Invoice-detail PDFs garbled (overlapping glyphs, collapsed spacing) | **Fixed — pending merge** (`fix/invoice-pdf-capture`) | `invoices/[id]/page.tsx` attached `printRef` to a node nested inside a `transform: scale(0.55)` preview wrapper (`:641-644` pre-fix). html2canvas reads geometry from the transformed (scaled-down) coordinate space while laying glyphs at natural font size → overlap + collapsed spacing in `viewPdf`/`downloadInvoice`/`resend`. Fix splits the concerns: the scaled block is now preview-only (no ref) and a second offscreen (`position:fixed; left:-9999`) unscaled node carries `printRef`, mirroring the clean chat finalize path (`chat/page.tsx:1909`). Chat + expense summary already captured offscreen at natural scale and were unaffected. `elementToPdf` also now awaits `document.fonts.ready` before capture (`lib/pdf/generate.ts:64`), removing the font-metric flake that the chat path masked with a 350 ms settle. Commits `0b71ecd`, `a241337`. |
| Migration drift: 8 local migrations unrecorded remotely; `get_public_invoice` dropped in prod | **Restore committed — pending apply** (`fix/migration-drift`) | `supabase migration list` shows 20260905000000, 20260910000000, 20260915000000, 20260916000000, and 20260918000000–000003 applied locally but not recorded remotely (some hand-run in the SQL editor). `public.get_public_invoice` was created by `20260901120000`, redefined by `20260910000000` (deleted_at guard), then `20260918000002` runs an **unconditional `drop function if exists` before its `create`**; if that create failed at hand-apply time (deposit_amount/amount_paid columns not yet present) the function is left dropped — matches the reported prod state. No app path calls it today (no `/pay` route; `/i/[token]` is referral-only, `src/app/i/[token]/route.ts`), so nothing live is broken, but any `supabase db push` or the eventual pay page will fail. Restore added as `20260918000004_restore_get_public_invoice.sql` (create-only via `create or replace`, `set search_path = ''` with everything schema-qualified incl. `extensions.pgp_sym_decrypt`, deleted_at + non-draft guards, grants service_role-only per `20260901120000`; every referenced column confirmed migration-created). Committed `9360d66` on `fix/migration-drift` — **not yet applied**. Live verification 2026-09-17 confirmed the drift: `get_public_invoice` absent (000010 ran, then 000002's DROP committed while its CREATE failed); soft_delete (6 objs), deposit_support (2 cols), deposits_and_partial_payments (2 cols), invoice_payments (7 objs), paid_status_from_ledger present; backfill_payments 0 'migrated' rows. **Repair, in order** (marks each hand-applied file as applied without re-running): `npx supabase migration repair --status applied <version>` for 20260905000000, 20260910000000, 20260915000000, 20260916000000, 20260918000000, 20260918000001, 20260918000002, 20260918000003. Marking 000010 and 000002 applied is safe because 000004 unconditionally recreates the function via `create or replace` on the next push. After repair, `db push` runs **only** 000004. Verified 2026-09-17. |
| `20260916000000` deposit_type check is a silent no-op | **Open (latent, no live break)** | `20260916000000_deposits_and_partial_payments.sql:3` re-adds `deposit_type` with a wider check (`in ('percent','percentage','fixed','none')`) via `add column if not exists`. Because `20260915000000_deposit_support.sql:3` already created the column with the narrower check (`'percentage','fixed','none'`), the `if not exists` makes the re-add a no-op and the wider constraint is never applied — `'percent'` is not permitted at the DB layer. App writes `'percentage'`, so no live breakage; the constraint the file appears to add is not in force. Live DB 2026-09-17: both deposit columns present with the narrower `20260915000000` check — the wider `'percent'` check from `20260916000000` never took effect, confirming the `add column if not exists` no-op. Verified 2026-09-16; live-confirmed 2026-09-17. |
| `DepositType` union permits `'percent'`, which the DB check rejects | **Open (latent, no live break)** | Audited 2026-09-17 (`fix/deposit-parsing`): `DepositType` (`src/lib/financials.ts:6`) includes `'percent'`, and the totals math (`financials.ts:90`) + PDF template (`pdf/templates/index.tsx:39,72`) read it defensively — but the live DB check allows only `percentage/fixed/none` (the `20260916000000` widening was a no-op, row above), so a `'percent'` **write** would fail the constraint. Confirmed nothing writes it: both deposit dropdowns emit only `none/percentage/fixed` (`chat/page.tsx:2092-2094`, `invoices/[id]/page.tsx:661-663`), finalize/`applyDeposit` write the dropdown value, and the new AI path emits only `'percentage'`. Safe today; the trap is the type advertising a value the DB won't accept. Fix later: drop `'percent'` from the union (keep the defensive reads) or widen the DB check to match. Verified 2026-09-17. |
| Share sheet "Copy" copies an unusable link | **Open (unverified)** | The native share sheet's Copy action reportedly yields a link that doesn't resolve to a viewable invoice. Consistent with there being no public invoice route (`shareInvoice` shares a File, not a URL — `lib/pdf/generate.ts:403`; see the migration-drift row). Needs a repro to pin down what the OS is actually copying (file URI vs. a stale/blob URL). Reported 2026-09-16. |

## Open — logged 2026-09-16 (Jules review verdicts)

_Jules's `REVIEW.md` (branch `jules-6296265834841558299-09e5b233`) was written
against main at `03ac630`, before the money-narration, tax-rate-unit, and
`fix/invoice-pdf-capture` merges. Each finding re-audited against current main;
severities below are ours and may differ from Jules's. Re-verify before acting._

| Finding | Status | Evidence |
|---|---|---|
| F1 — `resend()` forces `status:'sent'`, regressing a paid invoice | **Fixed — pending merge** (`fix/resend-status`) | `invoices/[id]/page.tsx` `resend()` wrote `status:'sent'` unconditionally (reachable via the always-visible "Share PDF" button `:441`), flipping a `'paid'` invoice back to `'sent'`. `reconcile_invoice_from_ledger` only fires on an `invoice_payments` change (`20260918000000`), so it stayed `'sent'` until the next ledger event — corrupting status-keyed dashboard/summary totals (see F2/F3). `/api/followups` would NOT nudge it (balance `total - amount_paid = 0` → skipped, `followups/route.ts:36-37`; `sent_at` also just reset). The old `inv.status !== 'sent'` snapshot gate also re-snapshotted a paid invoice. Fix: always bump `sent_at`; set status + `renderSnapshot` only when `inv.status === 'draft'`. Our severity **High** (Jules: Critical — reversible via any ledger touch, no false dunning). Commit `7453494`. |
| F5 — `resend()` no `try/finally` → UI stuck `busy` | **Fixed — pending merge** (`fix/resend-status`) | `setBusy(true)` cleared only on the happy path; a thrown `elementToPdf` or DB error stranded "Share PDF"/"Request balance"/"Download" disabled until reload. Wrapped the body in `try/finally`. Severity **Medium** (agrees with Jules). Commit `127c7ab`. |
| F2 — dashboard stats ignore `amount_paid` | **Fixed — pending merge** (`fix/cash-basis-income`) | Commit `0407267`. `dashboard/page.tsx` `loadStats` summed invoice face value (`Σ total where status='paid'` / `Σ total where sent/overdue`), so a $1000 invoice with a $400 deposit (`sent`) showed Collected $0 / Still owed $1000. Now cash basis: selects `amount_paid`; `collected = Σ amount_paid` over non-draft, `outstanding = Σ max(0, total - amount_paid)` over sent/overdue; `net = collected - spent`. Drafts + soft-deleted excluded. Verified + fixed 2026-09-17. |
| F3 — tax summary / Books income ignore `amount_paid` | **Fixed — pending merge** (`fix/cash-basis-income`) | Commit `0e4bbe0`. `summarizeIncome` summed raw `inv.total` for brought-in (paid) and still-owed (sent/overdue) and the query never selected `amount_paid`; same $1000/$400 case → broughtIn $0 / stillOwed $1000. Now cash basis: income (broughtIn + byClient) comes from `invoice_payments` rows bucketed by each payment's `paid_at`; **Still owed is as-of-today** (`Σ max(0, total - amount_paid)` over sent/overdue, period-independent, labeled "Still owed (as of today)"); the data-driven period list is fed by payment `paid_at` + expense `spent_on`. `summary/page.tsx` fetches the ledger (client name embedded, quotes/soft-deleted excluded) + the outstanding invoices; `InvoiceLite`/`summarizeIncome` reshaped, unused `invoiceRecordDate` removed. The summary **Export PDF is expenses-only and unchanged** (it never rendered income). Verified + fixed 2026-09-17. |
| Backfilled ledger `paid_at` may bucket old income to the migration date | **Resolved — no correction needed** (checked 2026-09-17) | `20260918000003` step 3 backfilled `invoice_payments.paid_at = coalesce(i.paid_at, i.updated_at)` for pre-ledger 'paid' invoices; when `paid_at` was null it fell back to `updated_at`, which can be the migration/edit date — so historical cash can land in Sept 2026 on the cash-basis summary. (`20260918000001` used `coalesce(sent_at, created_at)` and is less affected.) **Correction pending the check-query results** below; do not blind-update. **Do NOT anchor to `sent_at`** — it is overwritten on every share (`resend()` still bumps it), so it means "last shared", not "first sent". Propose anchoring to a value that does not move: `created_at`, or the earliest known sent date if one exists anywhere (e.g. `min(vault_documents.created_at)` for the invoice). Also consider adding a `first_sent_at` column (set once on first share, never overwritten) so future cash-basis anchoring has a stable date. Read-only check (run first): `select p.id, p.note, p.paid_at, i.invoice_number, i.status, i.created_at, i.sent_at, i.paid_at as invoice_paid_at, i.updated_at from public.invoice_payments p join public.invoices i on i.id=p.invoice_id where p.note in ('migrated','backfill: marked paid before ledger existed') order by p.paid_at desc;` **Result 2026-09-17: 0 suspect rows** — no backfilled `paid_at` fell back to `updated_at`/the migration date (consistent with `backfill_payments` having 0 'migrated' rows: no invoice carried `amount_paid > 0` before the ledger). No correction required; the anchoring guidance above is retained only if future backfills reintroduce the risk. Logged 2026-09-17. |
| F4 — `buildRenderData` returns inline unrounded subtotal/tax/total | **Open** (dup of existing row) | Same defect as "`buildRenderData` computes tax inline instead of `calculateTaxAmount`" above: `chat/page.tsx:716-720` returns inline `subtotal`/`taxAmount`/`total` (`:743-744`) instead of the `calculateInvoiceTotals` values computed at `:729`. Sub-cent drift, only with fractional qty/price, mostly masked by `money()`. Severity **Low–Medium** (Jules: High). Verified 2026-09-16. |
| F6 — `localDateToIso` local-midnight→UTC shifts payment dates | **Open (partly true, low impact)** | `invoices/[id]/page.tsx:31-34` stores local midnight as UTC (`paid_at`, used `:217`). Shifts only across differing record/display timezones or positive-UTC-offset users; round-trips correctly in a single negative-offset (US) zone, so ~no impact for the actual user profile. Fix if addressed: store date-only or anchor at `T12:00:00Z`. Severity **Low** (Jules: Medium). Verified 2026-09-16. |
| F7 — `confirmSummary` omits tax in announced total | **Closed** (by `14eee4c`, money narration) | No longer reproducible: `confirmSummary` now routes through `calculateInvoiceTotals` (`chat/page.tsx:778`) and announces `money(totals.total)` incl. tax + deposit (`:783-784`). Jules's cited `items.reduce` line is gone. No action. Verified 2026-09-16. |
| F8 — PDF issued vs due date format mismatch | **Fixed — pending merge** (`fix/small-batch`) | Both `issuedDate` and `dueDate` now go through `formatDate()` from `@/lib/dates` in `invoices/[id]/page.tsx` render data and `chat/page.tsx` `buildRenderData`, so the PDF reads one M/D/YYYY format. The dates are display strings only; `finalize()` writes the DB `due_date` (`date` column) from the raw ISO value, not the formatted string, so the column keeps ISO. Commit `4249ed7`. Severity **Low**. Fixed 2026-09-17. |

## Fixed + logged 2026-09-16 (chat card saves edits — branch `fix/chat-card-saves-edits`)

_The P1 "edits after the first save never reach the DB" bug, audited end to
end and fixed across five commits. New follow-up items logged below it._

| Item | Status | Evidence |
|---|---|---|
| Chat card edits dropped after first save (P1) | **Fixed — pending merge** (`fix/chat-card-saves-edits`) | Root cause: the parse handler nulled `pendingInvoiceRef` on every edit (audit "B1", `f154c49`), and the `finalize_key` idempotency guard (`74632fb`) then recovered the ORIGINAL row on the re-insert via a SELECT only — so edited line items / tax / total / deposit / notes were computed, rendered into the PDF, and thrown away. Send updated only `status`/`sent_at`/`renderSnapshot`; Download produced an edited PDF over an unchanged row. Fixed by a shared save path: `pendingInvoiceRef` now survives ordinary edits (one conversation = one invoice) and `finalize()` INSERTs on first save, UPDATEs the linked row by id on every later save (both Send and Download), from one `buildRenderData` payload, before rendering. The 23505 recovery is kept only as the reload/suspend safety net and re-applies the current draft to this conversation's own row. Commits `31b524d` (A), plus `ba8c15b` (F, buildRenderData via `calculateInvoiceTotals`) and `26865a4` (E, display-only Subtotal/Tax rows). |
| Sent/paid chat card must be read-only | **Fixed — pending merge** (`fix/chat-card-saves-edits`) | Commit B (`b068558`). The card had no knowledge of the linked row's live status (Q8). Now fetches `status` + `amount_paid` on restore, history open (re-links by `finalize_key`, rebuilding the card from the row via `draftFromRow` since finalized history stores `draft:null`), and at save time (hard refusal to update a non-draft row — the real backstop until the DB trigger below exists). Locked cards are read-only (line items, deposit, notes disabled; Send/Download/Undo/Change hidden) with a status badge, and chat edits to a locked draft are refused. |
| Revise a sent, unpaid invoice | **Fixed — pending merge** (`fix/chat-card-saves-edits`) | Commit C (`bd4fd93`). A locked `sent` invoice with `amount_paid = 0` shows "Revise": opens a NEW draft invoice in a fresh conversation seeded from the original (line items, tax, deposit, client, due date) plus a "Revises INV-XXXX" note; first save inserts a brand-new number and the original is untouched. Paid/partly-paid show a locked badge with no Revise. |
| Post-send live flow rebuilt a near-duplicate | **Fixed — pending merge** (`fix/chat-card-saves-edits`) | Commit B's lock only fired on restore/history reopen, not in the live flow: `finishFinalize` rotated `convoId` and cleared `pendingInvoiceRef`/`draft` after a Send, so a further chat message (e.g. "change the tax to 10%") hit `/api/parse` with the old transcript as history and `draft:null`, rebuilding the sent job as a NEW editable card (pencil edits lost) that a Download would insert as a near-duplicate #21. Fixed: `finishFinalize` now KEEPS the conversation linked and locked after Send (`pendingInvoiceRef`/`convoId`/`draft` retained, `linkedStatus='sent'`, `ready=true`, `finished=true`) and shows the locked card with Revise instead of resetting; `send()` refuses any further message in a locked conversation before `/api/parse` is called ("INV-XXXX is sent and locked. Tap Revise to change it, or start a new chat for a new job."). Done message, sent history entry, and the reminders opt-in are unchanged. |
| Locked (sent) conversation survives a reload | **Fixed — pending merge** (`fix/chat-card-saves-edits`) | Previously the sent conversation persisted only in memory: `finished=true` made the persist effect delete the chat slot, so a reload dropped the live locked card (the invoice stayed safe in the DB/history, but the in-session card was gone). Now `StoredChat` is v5 with `linkedStatus`/`linkedAmountPaid`; `finishFinalize` no longer clears the slot, the persist effect keeps a locked conversation even when `finished`, and `applyStoredChat` restores the lock state up front (and marks it `finished` to avoid re-archiving) with the DB status re-fetch still authoritative. After a reload the locked card + Revise show and further messages are refused, exactly as in the live flow; the "New chat" button still starts fresh. v3/v4 payloads migrate (restore unlocked until the DB re-fetch re-locks). |
| After Download, ask if it was sent → then archive + nudge | **Open (enhancement)** | Download saves a draft and skips the send side effects (status/`sent_at`/`renderSnapshot`, Vault archive, follow-up eligibility, reminders opt-in — `chat/page.tsx` download branch). Proposed: after a download, ask "Did you send it?"; on yes, run the same mark-sent + Vault archive + follow-up path a Send does. Deliberately out of scope for the P1 branch (Download's side effects were mapped, not changed). Logged 2026-09-16. |
| DB-level draft-only lock trigger | **Fixed — pending merge** (`fix/db-locks`) | `20260918000007_lock_sent_invoice_fields.sql` adds a `BEFORE UPDATE` trigger firing `when (old.status <> 'draft')` that pins the document fields: `subtotal`, `tax_rate`, `tax_amount`, `total`, `client_name`, `client_address`, `client_phone`, `notes`, `deposit_type`, `deposit_value`, `due_date`, `first_sent_at`, and the render-snapshot columns. Left writable (legit non-draft writers verified): `status`/`paid_at`/`amount_paid` (ledger reconcile), `sent_at` (resend), `deleted_at` (soft delete), `last_nudge_at` (followups). The draft→sent transition carries `OLD.status='draft'` so the trigger doesn't fire on it. `due_date` is now pinned (was writable only via an un-gated detail-page input — a UI oversight, since a changed due date wouldn't match the sent PDF); that input is gated to drafts in the same branch. See the 2026-09-17 db-locks section. Logged 2026-09-16; fixed 2026-09-17. |
| void / superseded status for revised originals | **Open (needs migration + status)** | Revise (Commit C) leaves the original `sent` and unchanged. A revised original has no marker that a newer version exists. Proposed: a `superseded` (or `void`) status set on the original when a revision is created, with a link to the revision. Needs a new status value + migration + UI, so deferred. Logged 2026-09-16. |
| Revise with payments = credit / refund | **Open (product)** | Revise is offered only for `sent` + `amount_paid = 0`. Once money has landed, changing the amount is a credit or refund, not an edit — a distinct feature (issue a credit note / record a refund against the ledger). Not built. Logged 2026-09-16. |
| Empty-state hint: a new job means a new chat | **Open (polish)** | "One conversation = one invoice" is now enforced (the change guard asks before rewriting a linked invoice; a new document comes from the compose/new-chat button). Add a light empty-state / first-run hint telling users a new job should start a new chat, so the change-guard question is rarely the first time they learn it. Logged 2026-09-16. |
| Tappable "New chat" in the "sent and locked" message | **Fixed — pending merge** (`fix/punch-batch-1`) | The locked-conversation refusal (`chat/page.tsx:716-722`) read "…Tap Revise to change it, or start a new chat for a new job" — Revise a real card button, but "start a new chat" only prose. Added an optional `Msg.action` (`'new-chat'`); the refusal message now carries it (copy reworded to "…Tap Revise on the card to change it, or start a new job.") and renders a "New chat" button beneath the bubble that dispatches `new Event('onit-new-chat')` — the exact event the header compose icon fires (`layout.tsx:157`). The field is spread through `withMessageIds`/`StoredChat`, so the button survives a reload of the locked conversation. Commit `654110c`. Fixed 2026-09-17. |
| Soft-delete cleanup of test invoices once soft-delete ships | **Open (manual, live DB)** | One-time cleanup once soft-delete ships (supersedes/absorbs the INV-0016 row above): remove the accumulated test invoices — **#18–22**, the **"Test A"** and **"Bob" test invoices** (created while testing the change-guard / new-invoice flow), and **INV-0016** (Cyril's account). Live-DB action, not code; use soft-delete so counts/anti-abuse cap behave as designed. Logged 2026-09-17. |

## Open — logged 2026-09-17 (soft-delete audit — branch `fix/soft-delete`)

_Soft-delete was found already merged to `main` (see the "Unmerged work to
port" row); this audit closed one leak, recorded the free-cap decision, and
logged two follow-ups. Re-verify before acting._

| Item | Status | Evidence |
|---|---|---|
| Vault showed soft-deleted invoices' PDFs | **Fixed — pending merge** (`fix/soft-delete`) | The Vault listed `vault_documents` unfiltered (`vault/page.tsx:24` pre-fix), so a soft-deleted invoice's archived PDF stayed visible and openable — the only read path that leaked deleted content (every other active read filters `deleted_at is null`). `vault_documents` has no `deleted_at` of its own but carries `invoice_id` (`001_init.sql:107`, `on delete set null`), which survives a soft delete. Fix embeds the parent invoice's `deleted_at` (`select('*, invoices(deleted_at)')`) and drops rows whose parent is deleted; receipts/other (`invoice_id` null → no embed) always stay. App-side only, no migration. Handles the embed as object or array. Commit `996dafe`. Fixed 2026-09-17. |
| Free user surprised that delete doesn't free a slot | **Resolved — copy added** (`fix/soft-delete`) | See the `enforce_free_invoice_limit` row above. Decision: the cap keeps counting soft-deleted invoices (anti-abuse, trigger + `access.ts` untouched); the delete confirm modal now carries one explanatory line for invoice deletes. Commit `996dafe`. |
| Paid invoices should be `void`, not deletable | **Open (needs migration + status)** | Q6 of the soft-delete audit: deleting a paid/partly-paid invoice (`amount_paid > 0`) silently drops its total from cash-basis Books/summary while the `invoice_payments` ledger rows persist — record and totals disagree. Today it is deletable behind the typed-DELETE gate (`DeleteConfirmModal.tsx`). Proposed: for `amount_paid > 0`, replace delete with a `void` status that keeps the record but excludes it. Overlaps the existing "void / superseded status for revised originals" and "Revise with payments = credit / refund" rows — fold together. Needs a status value + migration + UI. Logged 2026-09-17. |
| RLS grants `DELETE` — soft-delete is app-level only | **Fixed — pending merge** (`fix/db-locks`) | `20260918000008_split_invoice_expense_rls_no_delete.sql` replaces the `own invoices` / `own expenses` `for all` policies with separate `select`/`insert`/`update` policies, so `DELETE` is denied by default for the authenticated/anon roles. Verified nothing in the app issues a real DELETE on these tables (both soft-delete via `.update({ deleted_at })`); `handleDeletePayment` deletes `invoice_payments` rows, whose own `for all` policy is untouched; account deletion runs under the service role + FK cascade, unaffected. See the 2026-09-17 db-locks section. Logged 2026-09-17; fixed 2026-09-17. |

## Fixed — logged 2026-09-17 (Vault navigation — branch `fix/vault-nav`)

| Item | Status | Evidence |
|---|---|---|
| The Vault had no navigation entry | **Fixed — pending merge** (`fix/vault-nav`) | `/vault` was not a primary tab (`layout.tsx:22-30` — 4 tabs, Vault deliberately not among them) and had **no link from anywhere in the UI** except an archived PDF surfacing on an invoice's detail page. `getParentRoute('/vault')` already declared `/settings` as its Back parent (`layout.tsx:18`), but Settings offered no way in — so the Back target existed with no forward path. Fixed by making the Settings **RECORDS** section link the Vault: the `Expense summary` button (which pointed at `/summary`, already reachable from Books — a duplicate) is replaced with a `Vault` button → `/vault`, heading kept, description rewritten in the section's voice. `settings/page.tsx:623-628`. Commit `e454b8d`. Fixed 2026-09-17. |

## Fixed — logged 2026-09-17 (DB locks — branch `fix/db-locks`)

_Four database items (three from Jules's read-only review, verified against the
live DB; one first_sent_at follow-up). Audited against the code, then fixed as
four migrations (versions after `…004`) + one app-side commit. **Migrations not
yet applied** — the user runs `supabase db push`. Re-verify before acting._

| Item | Status | Evidence |
|---|---|---|
| Account deletion FK: `invoice_payments.user_id` NO ACTION | **Fixed — pending merge** (`fix/db-locks`) | `20260918000000` created `invoice_payments.user_id → auth.users(id)` with no ON DELETE clause (NO ACTION, `confdeltype='a'`) while `invoice_id` cascades. **Audit finding: the failure is NOT certain.** `/api/delete-account` deletes the `auth.users` row in one statement ([route.ts:137](src/app/api/delete-account/route.ts:137)); that cascades `auth.users → profiles → invoices → invoice_payments` (via `invoice_id` CASCADE), and because NO ACTION is an END-OF-STATEMENT check the payment rows are already gone when it runs, so the delete should succeed (the `profiles` FK is also older, firing before the `invoice_payments` check). Couldn't reproduce a certain 23503 from the schema. Fixed regardless — `20260918000005_cascade_invoice_payments_user.sql` sets `on delete cascade`, matching `invoice_id` and removing the timing dependence. |
| Sent invoices not locked at the DB layer | **Fixed — pending merge** (`fix/db-locks`) | `20260918000007_lock_sent_invoice_fields.sql` — see the "DB-level draft-only lock trigger" row above for the pinned/writable column split. Wider pin list (snapshot + deposit + due_date) chosen by the user. |
| RLS grants DELETE on invoices/expenses | **Fixed — pending merge** (`fix/db-locks`) | `20260918000008_split_invoice_expense_rls_no_delete.sql` — see the "RLS grants DELETE" row above. |
| `first_sent_at` — sent_at means "last shared", not "first sent" | **Fixed (column) — pending merge** (`fix/db-locks`) | `20260918000006_invoice_first_sent_at.sql` adds `invoices.first_sent_at`, backfilled from `sent_at`, stamped write-once by `set_first_sent_at` on both the draft→sent UPDATE and an INSERT born non-draft (a born-sent row the UPDATE trigger would miss), and pinned by the lock trigger. **The read that should switch to it is [`api/followups/route.ts:30`](src/app/api/followups/route.ts:30)** `.lt('sent_at', cutoff)` — a resend bumps `sent_at` and silences the 2-day dunning window. **Read switched 2026-09-17 (`fix/deposit-parsing`, `ac97102`):** followups now filters `` .or(`first_sent_at.lt.${cutoff},and(first_sent_at.is.null,sent_at.lt.${cutoff})`) `` — dun on `first_sent_at`, fall back to `sent_at` only for a legacy sent row whose `first_sent_at` is null (backfill skips rows with a null `sent_at`); both null → not dunned, matching the prior `sent_at`-only behavior. Only read of `sent_at` meaning "first sent" (trial-reminders uses `trial_reminder_sent_at`; the other two `sent_at` hits are the send/resend writes). Needs the column migration applied. |
| Detail-page due-date input not draft-gated | **Fixed — pending merge** (`fix/db-locks`) | The `[id]/page.tsx` due-date `<input>` ([:586](src/app/(app)/invoices/[id]/page.tsx:586) pre-fix) wasn't `isDraft`-gated, so it accepted edits on a sent invoice — a UI oversight, and now a silent no-op once the lock trigger pins `due_date`. Gated to drafts; a sent invoice shows its due date read-only (`formatDate`). App-side commit (its own), typecheck clean. |

## Extras — incomplete things found in passing (not on the list)

- **Middleware restored as refresh-only** (see the middleware Bug row) — after the relocation loop (the dropped-cookie bug) got the file deleted, `src/middleware.ts` is back as a refresh-only `@supabase/ssr` updateSession with **no redirect branch**. No server-side route gating runs; RLS is the sole boundary (audited intact). Push check: `Test-Path src/middleware.ts` = True.
- **Stripe Connect stub** — `settings/page.tsx` renders a disabled "Coming soon" button; comment: "Layout only; no Stripe connect logic yet."
- **Dead ref/TODO** — `chat/page.tsx:287`: `// TODO: sessionRef unused — per-message source replaced the speak gate.`
- **Unused public-invoice backend** — `get_public_invoice` + invoice token in migration `20260901120000` have no caller (tie-in to the missing pay page). **Update 2026-09-18:** `get_public_invoice` now has its first caller — the read-only pay page `/pay/[token]` (`fix branch feat/pay-page-read`, pushed for preview, not yet merged). The function itself was confirmed live in prod by the user (exists, `prosecdef` true, `search_path ''`, service_role only), so the migration-drift restore (`20260918000004`) is applied.
- **Duplicate payment-handle URL builders** — `cashAppUrl` / `payPalUrl` / `venmoUrl` are defined in both `src/lib/pdf/templates/index.tsx:84-87` and `src/app/pay/[token]/PayView.tsx` (kept local so the pay page doesn't import the PDF render module). Same drift risk as the nine `money()` copies. Consolidate into a shared `src/lib/payment-handles.ts` in a later branch. Logged 2026-09-18 (`feat/pay-page-read`).
